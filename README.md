A TCP and WebSocket tunnel via Yandex Cloud (using Serverless Functions and API Gateway, and there's also an experimental branch that uses QUIC protocol machanics over WS) to bypass Russian internet censorship under strict whitelist filtering.

See README_RU.md for documentation in Russian.